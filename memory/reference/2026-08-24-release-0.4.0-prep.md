# Release 0.4.0 Prep

Date: 2026-08-24

## External Sources

- None. Release prep was based on local repository files and user direction.

## Direct References

- `pyproject.toml`: package version metadata.
- `CHANGELOG.md`: root release notes and version history.
- `docs/changelog.md`: MkDocs release notes and version history.
- `docs/index.md`: MkDocs landing page feature overview.
- `docs/features.md`: MkDocs use-case and feature overview.
- `README.md`: public package overview, installation, and usage notes.

## Generated Parts

- Bumped package version from `0.3.0` to `0.4.0`.
- Moved the prepared 0.4.0 changelog notes into dated release sections.
- Added README coverage for streaming replies, `MIMINIONS_HOME`, verbose chat observability, and `SQLiteMemory` context manager usage.
- Added top-level docs coverage for streaming replies, resilient runs, `MIMINIONS_HOME`, and `SQLiteMemory` context-manager examples.
- Added markdownlint MD024 disables to changelog files because repeated `Added` / `Changed` headings are expected in changelog format.
- Added markdownlint exceptions for MkDocs Material syntax where needed.
- Normalized touched markdown table separator spacing.

## Decisions

- Left module docs untouched per user request to complete items 1-4 only.
- Later updated only top-level docs pages after user asked about docs; module docs already covered 0.4.0 behavior.
- Left `src/miminions/__init__.py` unchanged because runtime `__version__` is read from installed package metadata.
- Left deploy scripts unchanged because GitHub pipeline owns publishing.

## Validation

- `uv build` succeeded after edits.
- `uv run mkdocs build --strict` succeeded after docs edits.
- Final artifacts built: `dist/miminions-0.4.0.tar.gz` and `dist/miminions-0.4.0-py3-none-any.whl`.
- Editor diagnostics reported no errors for `pyproject.toml`, `CHANGELOG.md`, `docs/changelog.md`, `README.md`, `docs/index.md`, or `docs/features.md` after cleanup.
