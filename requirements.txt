# Development dependency entry point.
#
# `pyproject.toml` is the single source of truth for dependencies. This file
# only exists so that `pip install -r requirements.txt` keeps working; it
# installs the project in editable mode with every optional extra.
#
# Do not list packages here. Add them to the appropriate extra in
# `pyproject.toml` instead, or this file will drift out of sync again.

-e .[all,dev,docs,cli-build]
